[pytest]
# integration: 외부 Gateway API 또는 DB가 필요한 테스트. 기본 실행에서 제외하고
#              명시적으로 `pytest -m integration`으로만 실행 (결정론적/오프라인 기본 보장)
markers =
    integration: requires external Gateway API or DB
addopts = -m "not integration"
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
