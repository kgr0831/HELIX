# Cross-Check AI 개발 기획서

> **이기종 SLM 기반 Multi-Agent System을 통한 Single-Agent 하이엔드 LLM과의 비교 연구**
>
> AI소프트웨어학부 김가람

---

## 1. 연구 정체성

### 1.1 연구 질문
> **"동일 thinking token 환경에서, 이기종 SLM으로 구성된 MAS가 하이엔드 단일 SAS와 비교했을 때 우위는 어디에 있는가?"**

### 1.2 연구 배경 (선행 연구 지도)

| 논문 | 주장 | 한계 |
|---|---|---|
| **X-MAS** (Ye et al., 2025, arXiv:2505.16997) | 이기종 MAS가 동종 MAS보다 우수 | 대형 모델, 자원 제약 없음, SAS 비교 없음 |
| **Yang et al. (2026, arXiv:2602.03794)** | 다양성(K*)이 MAS 성능을 결정, 이종 2 agents ≥ 동종 16 agents | SAS와 정면 비교 없음, 비슷한 체급 모델만 |
| **Tran & Kiela (2026, arXiv:2604.02460, Stanford)** | 동일 token budget에서 SAS ≥ 동종 MAS (DPI 논증) | **동종 MAS만 다룸 — 이기종 MAS 미검증** |

### 1.3 본 연구의 위치 (빈 지점)

```
        [X-MAS]                      [Yang et al.]
   이기종 MAS > 동종 MAS         이기종 MAS > 동종 MAS
   (대형, 제약 없음)              (이론적 framework)
                \\                    //
                 \\                  //
                  \\   본 연구      //
                   \\  여기 위치   //
                    \\            //
                     [Tran & Kiela]
                  SAS ≥ 동종 MAS
                  (동종만 검증함)
```

**Tran & Kiela의 결론은 동종 MAS에 한정된다. 이기종 MAS에서는 어떻게 될까?**

### 1.4 이론적 충돌

두 이론이 이기종 MAS vs SAS에 대해 **서로 다른 예측**을 함:

- **Tran & Kiela (DPI)**: agent 간 통신은 정보 손실 → 이종이든 동종이든 MAS는 SAS에 짐
- **Yang et al. (Effective Channel)**: 다양성이 비중복 정보 채널을 늘림 → 이종 MAS는 SAS를 이길 수 있음

본 연구가 이 충돌의 arbiter 역할을 함.

---

## 2. 시스템 개요: Cross-Check AI

### 2.1 정체성
**Cross-Check AI는 연구의 결과물이 아니라 실험 플랫폼이다.** 시스템 설계 기준은 "예쁜 데모"가 아니라 **"Tran & Kiela 실험을 그대로 재현 가능 + 이기종 조건으로 변경 가능"**.

### 2.2 4-Agent 이기종 구성

| 역할 | 모델 | 위치 | 강점 | 비고 |
|---|---|---|---|---|
| **Leader** | Qwen2.5-7B-Instruct (Q4_K_M) | 로컬 GPU (Ollama) | 균형잡힌 통합 능력 | 4bit quantized, ~5GB VRAM |
| **Researcher** | Gemini 2.5 Flash | Google AI Studio API | 긴 컨텍스트, 빠른 응답 | 무료 분당 15요청 |
| **Logician** | DeepSeek-R1-Distill-Llama-70B | Groq API | 추론 특화 (R1 distillation) | 무료 분당 30요청 |
| **Critic** | Phi-3.5-mini-3.8B (Q4_K_M) | 로컬 GPU (Ollama) | 가볍고 빠른 반박/엣지케이스 | ~2.5GB VRAM |

**이기종성 4축 확보**:
1. 학습 데이터 다양성 (Alibaba / Google / DeepSeek / Microsoft)
2. 아키텍처 다양성 (dense / dense / dense / dense — 일단은 dense 통일)
3. 체급 다양성 (7B / 미공개 / 70B-distill / 3.8B)
4. 기능 특화 다양성 (범용 / 컨텍스트 / 추론 / 압축비판)

→ Yang et al.의 K* (effective channel) 관점에서 다양성이 보장됨

### 2.3 SAS Baseline 구성

비교 대상 SAS는 두 종류:

| Baseline | 모델 | 목적 |
|---|---|---|
| **단일 SAS (강)** | DeepSeek-R1-Distill-Llama-70B (Groq) | "하이엔드 단일 모델" 대표 |
| **단일 SAS (동급)** | Qwen2.5-7B-Instruct (로컬) | 자원 매칭된 단일 모델 비교 |
| **단일 SAS (참조)** | Gemini 2.5 Flash | 상용 API 단일 비교 |

---

## 3. 시스템 아키텍처

### 3.1 전체 구조

```
┌─────────────────────────────────────────────────────────┐
│  Client (React + Vite + Tailwind)                       │
│   - Glass Box UI: 4-Agent 토론 실시간 시각화           │
│   - Multi-hop QA 입력 + 결과 표시                       │
└────────────────────┬────────────────────────────────────┘
                     │ HTTP / SSE
┌────────────────────▼────────────────────────────────────┐
│  Backend (FastAPI + LangGraph)                          │
│   ┌──────────────────────────────────────────────────┐ │
│   │  LangGraph Orchestrator                          │ │
│   │   - State 관리 (토론 로그, agent별 응답)         │ │
│   │   - 조건부 라우팅 (합의/불일치 분기)             │ │
│   │   - Async 4-agent 호출                           │ │
│   └──────────────────────────────────────────────────┘ │
│   ┌──────────────────────────────────────────────────┐ │
│   │  Token Budget Controller                         │ │
│   │   - 각 agent의 thinking token 제한               │ │
│   │   - Tran & Kiela와 동일한 budget matching       │ │
│   └──────────────────────────────────────────────────┘ │
│   ┌──────────────────────────────────────────────────┐ │
│   │  Context Compressor                              │ │
│   │   - 토론 로그 압축 (불필요 미사여구 제거)        │ │
│   │   - 비용 폭발 방지                               │ │
│   └──────────────────────────────────────────────────┘ │
└─────┬───────────┬───────────┬───────────┬──────────────┘
      │           │           │           │
┌─────▼─────┐ ┌──▼──────┐ ┌──▼──────┐ ┌──▼──────────┐
│  Ollama   │ │ Gemini  │ │  Groq   │ │  Ollama     │
│  Qwen2.5  │ │ 2.5 F   │ │ R1-70B  │ │  Phi-3.5    │
│  (Leader) │ │ (Resrch)│ │ (Logic) │ │  (Critic)   │
└───────────┘ └─────────┘ └─────────┘ └─────────────┘
```

### 3.2 데이터 플로우 (Parallel-Roles 아키텍처)

```
[입력: Multi-hop 질문]
         │
         ▼
[Leader: 작업 분배 + 컨텍스트 정리]
         │
         ▼
[3-Agent 병렬 호출 (asyncio)]
   ├─ Researcher: 사실 추출
   ├─ Logician: 논리 검증
   └─ Critic: 엣지 케이스/반박
         │
         ▼
[Leader: 응답 통합 + 합의 확인]
         │
    ┌────┴────┐
    │         │
[합의 ⭕]  [불일치 ❌]
    │         │
    ▼         ▼
[최종답]  [재토론 1회]
              │
              ▼
        [Leader: 최종 결정]
```

### 3.3 Token Budget 제어 메커니즘

Tran & Kiela 논문 핵심: **공정한 비교를 위해 thinking token을 동일하게 맞춤**

```python
# 예시 budget matching
SAS_BUDGET = 8000 tokens  # 단일 모델 추론 예산

# MAS의 경우 4개 agent 합쳐서 동일 예산
MAS_BUDGET_PER_AGENT = {
    "leader": 2000,      # 통합 비용
    "researcher": 2000,  # 사실 추출
    "logician": 2000,    # 논리 검증
    "critic": 2000,      # 비판
}
# 합 = 8000 tokens (SAS와 동일)
```

토큰 카운팅은 `tiktoken` 라이브러리로 직접 측정 (API 제공 값 신뢰 X — Tran & Kiela도 Gemini API의 token accounting 아티팩트를 지적함).

---

## 4. 데모 시나리오 (Multi-hop QA 시각화)

### 4.1 입력 예시

```
질문: "Christopher Nolan이 감독한 영화 중 가장 높은 흥행 수익을 
       기록한 영화의 음악을 작곡한 사람이 작업한 다른 크리스토퍼 놀란 
       영화는 무엇인가?"

정답: The Dark Knight (Hans Zimmer 작곡)
필요 hop:
  1. Nolan 영화 중 최고 흥행 → The Dark Knight Rises
  2. 작곡가 → Hans Zimmer
  3. Zimmer가 작업한 다른 Nolan 영화 → Inception, Dunkirk, Interstellar 등
```

### 4.2 Glass Box UI 흐름

```
사용자 입력 → [Researcher가 사실 검색 중...]
              [Logician이 논리 검증 중...]
              [Critic이 반박 중...]
              [Leader가 통합 중...]
              
              실시간 토론 로그:
              💬 Researcher: "Nolan 최고 흥행은 TDKR ($1.08B)"
              💬 Logician: "확인. Zimmer가 음악 담당."
              💬 Critic: "Inception도 Zimmer 작품 아닌가?"
              💬 Leader: "둘 다 맞음. Zimmer가 작업한 
                          다른 Nolan 영화: Inception, Dunkirk..."
              
              ✅ 최종 답: Inception (또는 사용자 의도에 따라)
```

PDF 와이어프레임의 "Glass Box UI" 컨셉 그대로 활용.

### 4.3 데이터셋

- **MuSiQue** (메인): 2~4-hop 추론 질문, 정답 명확, 자동 채점 가능
  - 다운로드: https://github.com/StonyBrookNLP/musique
  - Subset: `musique_ans_v1.0_dev.jsonl` (개발용)
- **FRAMES** (선택): 후속 실험용, 검색 기반 multi-hop

---

## 5. 평가 프로토콜

### 5.1 평가 차원

| 차원 | 메트릭 | 측정 방법 |
|---|---|---|
| **정확도** | Exact Match (EM), F1 | MuSiQue 표준 평가 |
| **Token 효율성** | accuracy / total tokens | 자체 계산 |
| **Latency** | wall-clock time (초) | Python `time` |
| **API 호출 수** | total calls | 카운터 |
| **재현성** | 동일 입력 3회 반복 평균 | seed 고정 |

### 5.2 비교 대상 (실험 셀)

| Cell | 시스템 | Token Budget | 목적 |
|---|---|---|---|
| **C1** | SAS: DeepSeek-R1-70B 단독 | 8000 | 하이엔드 SAS baseline |
| **C2** | SAS: Gemini 2.5 Flash 단독 | 8000 | 상용 API baseline |
| **C3** | SAS: Qwen2.5-7B 단독 | 8000 | 동급 자원 baseline |
| **C4** | 동종 MAS: Qwen2.5-7B × 4 | 8000 (2000×4) | Tran & Kiela 재현 |
| **C5** | **이종 MAS: Cross-Check AI** | 8000 (2000×4) | **메인 실험** |

### 5.3 예상 결과 시나리오

| 시나리오 | C5 vs C1 | 해석 |
|---|---|---|
| **A** | C5 > C1 | 이기종성이 체급 열세를 상쇄. Yang et al. 지지. |
| **B** | C5 ≈ C1 | 이기종성이 일부 상쇄. 중간 결과. |
| **C** | C5 < C1 | Tran & Kiela 결과가 이기종에도 확장됨. DPI 지지. |
| **D** | task별로 다름 | **가장 가능성 높음**. 조건부 매핑이 contribution. |

어떤 시나리오든 논문이 됨 (탐색 질문으로 설계되었기 때문).

---

## 6. 기술 스택

### 6.1 백엔드

| 도구 | 용도 | 비용 |
|---|---|---|
| Python 3.11+ | 메인 언어 | 무료 |
| FastAPI | 웹 API | 무료 |
| LangGraph | Multi-Agent 오케스트레이션 | 무료 |
| LangChain | LLM 통합 | 무료 |
| Ollama | 로컬 LLM 추론 | 무료 |
| tiktoken | 토큰 카운팅 | 무료 |
| Pydantic | 데이터 검증 | 무료 |
| SSE-Starlette | Server-Sent Events (실시간 스트리밍) | 무료 |

### 6.2 프론트엔드

| 도구 | 용도 | 비용 |
|---|---|---|
| React 18 + TypeScript | UI 프레임워크 | 무료 |
| Vite | 빌드 도구 | 무료 |
| Tailwind CSS | 스타일링 | 무료 |
| Zustand | 상태 관리 | 무료 |
| shadcn/ui | UI 컴포넌트 | 무료 |

### 6.3 LLM API (전부 무료 티어)

| 서비스 | 모델 | 무료 한도 |
|---|---|---|
| **Google AI Studio** | Gemini 2.5 Flash | 분당 15 RPM, 일일 1500 RPD |
| **Groq** | DeepSeek-R1-Distill-Llama-70B | 분당 30 RPM, 일일 14400 RPD |
| **Ollama (로컬)** | Qwen2.5-7B, Phi-3.5-mini | 무제한 (하드웨어 한계까지) |

### 6.4 개발/평가 도구

| 도구 | 용도 |
|---|---|
| VSCode + Claude Code (PDF에 명시) | IDE |
| GitHub + GitHub Actions | 버전관리, CI |
| PyTest | 단위 테스트 (PDF에 명시) |
| Pandas + Matplotlib | 결과 분석/시각화 |
| Google Colab (선택) | 추가 실험 (T4 GPU 16GB) |

---

## 7. 하드웨어 자원 계획

### 7.1 사양 검토

| 자원 | 사양 | Cross-Check AI에서의 역할 |
|---|---|---|
| CPU: Ryzen9 8940HX | 16C/32T | 백엔드 서버, 토큰 카운팅, 임베딩 |
| GPU: RTX 5060 Laptop | 8GB VRAM | 로컬 모델 추론 (Leader, Critic) |
| RAM: 32GB DDR5 | 충분 | LangGraph state, 컨텍스트 캐시 |
| 내장 GPU (610M) | 미사용 | — |

### 7.2 VRAM 예산 (8GB)

```
Qwen2.5-7B-Q4_K_M:    ~4.8 GB
Phi-3.5-mini-Q4_K_M:  ~2.4 GB
KV 캐시 + 여유:        ~0.8 GB
─────────────────────────────
합계:                  ~8.0 GB
```

**동시 구동 가능** — Ollama가 알아서 메모리 관리. 만약 OOM 발생 시 순차 실행으로 fallback.

### 7.3 Colab 활용 시나리오

Colab T4 (16GB VRAM) 무료 티어 활용:
- 더 큰 로컬 모델 실험 (e.g., DeepSeek-Coder-V2-Lite 16B)
- 배치 평가 실행 (수백 개 MuSiQue 샘플)
- 4시간 세션 제한 → 평가 스크립트만 실행 (서빙 X)

---

## 8. 개발 일정 (PDF 마일스톤 구체화)

### 4월: 설계 및 환경 구축

| 주차 | 작업 |
|---|---|
| 1주차 | Ollama 설치, Qwen2.5/Phi-3.5 로컬 구동 검증 |
| 2주차 | Gemini API, Groq API 키 발급 및 호출 테스트 |
| 3주차 | LangGraph 학습, 단순 2-agent 프로토타입 |
| 4주차 | MuSiQue 데이터셋 로드, baseline SAS 평가 스크립트 |

### 5월: 코어 로직 개발 + 중간 결과물

| 주차 | 작업 |
|---|---|
| 1주차 | 4-Agent LangGraph 그래프 구현 (Parallel-roles) |
| 2주차 | Token budget 제어 메커니즘, Context compressor |
| 3주차 | 단위 테스트 (PyTest), 작은 샘플 (50개) 평가 |
| 4주차 | **중간 결과물 제출** — C1, C3, C5 셀 비교 (50샘플) |

### 6월: UI 연동 + 최종 검증

| 주차 | 작업 |
|---|---|
| 1주차 | FastAPI + SSE 백엔드, React UI 연동 |
| 2주차 | Glass Box UI 완성, 실시간 토론 스트리밍 |
| 3주차 | 전체 평가셋 (500+ 샘플) 실행, 5개 셀 비교 |
| 4주차 | **최종 검증 및 보고** — 논문 초안, 데모 영상 |

---

## 9. 리스크 관리

### 9.1 기술적 리스크

| 리스크 | 가능성 | 영향 | 대응 |
|---|---|---|---|
| API 무료 한도 초과 | 중 | 중 | Retry + 시간 분산, 평가는 시간차 배치 실행 |
| VRAM OOM | 중 | 중 | 순차 실행 fallback, 더 작은 quantization (Q3) |
| Groq 응답 지연/장애 | 중 | 중 | 로컬 모델로 fallback (DeepSeek-R1-Distill 7B) |
| LangGraph state 폭증 | 저 | 중 | Context compressor, 토론 라운드 제한 (최대 3) |
| Token 카운팅 불일치 | 중 | 고 | tiktoken 자체 측정, API 값 신뢰 X |

### 9.2 연구적 리스크

| 리스크 | 가능성 | 영향 | 대응 |
|---|---|---|---|
| 이종 MAS도 SAS에 짐 | 중 | 저 | "조건부 매핑"으로 프레이밍 (탐색 질문) |
| 결과 noise 큼 | 중 | 중 | 3회 반복 평균, 통계 검정 |
| MuSiQue 결과가 일반화 안 됨 | 저 | 중 | FRAMES도 추가 실험 (6월) |
| Tran & Kiela 후속 논문 선점 | 저 | 고 | 빠른 진행, arXiv 선공개 고려 |

### 9.3 PDF 명시 리스크 ([Risk] 비용 폭발)

PDF가 짚은 "에이전트 간 토론 장기화 → API 토큰 증가"에 대한 대응:

- **합의 종료 조건**: 모든 agent 응답 유사도 > 0.85 (cosine) → 즉시 종료
- **최대 라운드 제한**: 3회 토론 후 강제 종료
- **Context compression**: 매 라운드마다 핵심 결론만 추출
- **Token 모니터링**: 실시간 카운터, threshold 도달 시 alert

---

## 10. 폴더 구조 (제안)

```
cross-check-ai/
├── README.md
├── PROPOSAL.md (← 이 문서)
├── requirements.txt
├── .env.example
├── .gitignore
│
├── backend/
│   ├── main.py                    # FastAPI 진입점
│   ├── agents/
│   │   ├── __init__.py
│   │   ├── base.py                # Agent 추상 클래스
│   │   ├── leader.py              # Qwen2.5 (Ollama)
│   │   ├── researcher.py          # Gemini 2.5 Flash
│   │   ├── logician.py            # DeepSeek-R1-Distill (Groq)
│   │   └── critic.py              # Phi-3.5 (Ollama)
│   │
│   ├── graph/
│   │   ├── __init__.py
│   │   ├── state.py               # LangGraph State 정의
│   │   ├── nodes.py               # 노드 함수들
│   │   ├── edges.py               # 조건부 라우팅
│   │   └── builder.py             # 그래프 빌드
│   │
│   ├── utils/
│   │   ├── token_counter.py       # tiktoken 래퍼
│   │   ├── context_compressor.py  # 미사여구 제거
│   │   └── budget_controller.py   # Token budget 매칭
│   │
│   └── api/
│       ├── routes.py              # FastAPI 엔드포인트
│       └── sse.py                 # 실시간 스트리밍
│
├── frontend/
│   ├── src/
│   │   ├── App.tsx
│   │   ├── components/
│   │   │   ├── GlassBox.tsx       # 토론 로그 UI
│   │   │   ├── AgentBubble.tsx    # agent별 응답 표시
│   │   │   └── InputForm.tsx
│   │   └── hooks/
│   │       └── useSSE.ts          # SSE 연결
│   ├── package.json
│   └── vite.config.ts
│
├── experiments/
│   ├── datasets/
│   │   └── musique/               # MuSiQue 다운로드 위치
│   ├── baselines/
│   │   ├── run_sas_deepseek.py   # C1
│   │   ├── run_sas_gemini.py     # C2
│   │   ├── run_sas_qwen.py       # C3
│   │   └── run_homog_mas.py      # C4
│   ├── main/
│   │   └── run_cross_check.py    # C5
│   ├── eval/
│   │   ├── metrics.py             # EM, F1
│   │   └── compare_cells.py       # 결과 비교
│   └── results/
│       └── (CSV, JSON 결과 저장)
│
├── tests/
│   ├── test_agents.py
│   ├── test_budget.py
│   └── test_graph.py
│
└── docs/
    ├── architecture.md
    └── api.md
```

---

## 11. 초기 셋업 가이드

### 11.1 환경 구축 (Windows + WSL2 권장)

```bash
# 1. 가상환경
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 2. 패키지 설치
pip install fastapi uvicorn langgraph langchain langchain-google-genai \
            langchain-groq langchain-ollama pydantic python-dotenv \
            tiktoken sse-starlette pandas matplotlib pytest

# 3. Ollama 설치 (https://ollama.com)
# Windows: 설치 파일 다운로드
# Linux: curl -fsSL https://ollama.com/install.sh | sh

# 4. 모델 다운로드
ollama pull qwen2.5:7b-instruct-q4_K_M
ollama pull phi3.5:3.8b-mini-instruct-q4_K_M

# 5. API 키 설정 (.env 파일)
# GOOGLE_API_KEY=...     ← https://aistudio.google.com 에서 발급
# GROQ_API_KEY=...       ← https://console.groq.com 에서 발급

# 6. MuSiQue 데이터셋
cd experiments/datasets
git clone https://github.com/StonyBrookNLP/musique.git
cd musique && bash download_data.sh
```

### 11.2 첫 동작 확인

```bash
# 백엔드 실행
cd backend
uvicorn main:app --reload --port 8000

# 프론트엔드 실행 (다른 터미널)
cd frontend
npm install && npm run dev

# 브라우저: http://localhost:5173
```

---

## 12. 경제성 분석 (PDF 컨셉 유지, 무료 버전)

### 12.1 PDF 원본 vs 무료 버전 비교

| 항목 | PDF 원본 컨셉 | 무료 개발 버전 |
|---|---|---|
| Leader | Claude Haiku 4.5 ($5/1Mtok) | Qwen2.5-7B (로컬, $0) |
| Researcher | Gemini 2.5 Flash ($2.5/1Mtok) | Gemini 2.5 Flash (무료 티어) |
| Logician | DeepSeek V3.2 ($1.1/1Mtok) | DeepSeek-R1-Distill (Groq 무료) |
| Critic | Gemini 2.5 Flash-Lite ($0.4/1Mtok) | Phi-3.5-mini (로컬, $0) |
| **총 비용** | **약 $1.5 / 1Mtok** | **$0** |
| 비교 대상 (SAS) | Claude Opus 4.6 ($25/1Mtok) | DeepSeek-R1-70B (Groq 무료) |

→ **개발 단계에서는 100% 무료**, 발표 시 "프로덕션은 PDF 컨셉대로 갈 수 있음"으로 어필.

---

## 13. 발표/논문 차별점 요약

1. **Tran & Kiela 직접 반박/확장**: Stanford 논문의 한계를 명확히 지적하고 실증
2. **두 이론의 arbiter**: DPI vs Effective Channel의 충돌을 실험으로 판가름
3. **PDF의 비용 효율 메시지 + 학술 contribution 동시 확보**: 발표 임팩트 + 논문 의의
4. **재현 가능성**: 전부 오픈소스 + 무료 API → 누구나 검증 가능
5. **Cross-Check AI는 도구이자 데모**: 연구 플랫폼이면서 그 자체로 보여줄 수 있음

---

## 14. 다음 액션 (즉시)

1. **GitHub repo 생성** + 이 기획서를 PROPOSAL.md로 commit
2. **Ollama 설치 + Qwen2.5-7B 로컬 구동 확인** (1시간)
3. **Gemini, Groq API 키 발급** (30분)
4. **MuSiQue 데이터셋 다운로드** (15분)
5. **단일 agent 호출 테스트 스크립트 작성** — 4개 모델 각각 "Hello" 응답 받기 (2시간)
6. 위 5번 성공 시 → LangGraph 학습 시작

---

## 15. 서비스화 확장 (Productization Layer)

### 15.1 배경

본 시스템은 연구 플랫폼인 동시에 데모 서비스다(2.1). 데모를 실제 서비스 형태로
보여주기 위해 **인증·영구 저장·비용 절감** 3개 계층을 추가한다. 이 계층은 연구
실험 파이프라인(experiments/)과 독립적이며, 실험 재현성에 영향을 주지 않는다.

| 기능 | 목적 | 비연동 시 현재 상태 |
|---|---|---|
| Google OAuth 로그인 | 사용자 식별, 세션 관리 | `useAuth.ts`의 `test/test` 인메모리 하드코딩 |
| DB 연동 (PostgreSQL) | 계정·대화·API키·사용량 영구 저장 | 전부 더미 데이터 / 휘발성 |
| 답변 캐싱 (임베딩 유사도) | 중복 토론 비용·지연 절감 | 매 질문마다 4-Agent 토론 전체 재실행 |

### 15.2 Google OAuth 2.0 로그인

- **방식**: Authorization Code Flow (Authlib + FastAPI). 기존 `test/test` 로그인과
  이메일/비밀번호 가입 폼은 **제거**하고 Google 로그인으로 일원화한다.
- **흐름**:
  ```
  [프론트] "Google로 계속하기" 클릭
        → GET /api/auth/google/login  (백엔드가 Google 동의 화면으로 redirect)
        → 사용자 동의
        → GET /api/auth/google/callback?code=...  (백엔드가 code→token 교환, userinfo 조회)
        → users 테이블 upsert
        → JWT 발급 (HttpOnly 쿠키)
        → 프론트 /chat 으로 redirect
  ```
- **세션**: 단기 access JWT + 갱신용 refresh 토큰. 프론트 `useAuth`는 `/api/auth/me`로
  로그인 상태를 확인한다.

### 15.3 DB 연동 (PostgreSQL + SQLAlchemy)

- **스택**: PostgreSQL 16 + SQLAlchemy 2.0(async, asyncpg) + Alembic(마이그레이션) +
  `pgvector` 확장(15.4 캐싱용).
- **스키마(초안)**:

| 테이블 | 핵심 컬럼 | 용도 |
|---|---|---|
| `users` | id, email, name, picture, provider, provider_user_id, created_at | 계정(Google 프로필) |
| `conversations` | id, user_id(FK), title, created_at | 대화 세션 |
| `messages` | id, conversation_id(FK), question, final_answer, consensus, token_usage(JSON), latency_ms, created_at | 질문/최종답변 |
| `agent_events` | id, message_id(FK), round_number, agent_name, role, content | 토론 로그(discussion_log) 영구화 |
| `api_keys` | id, user_id(FK), provider, encrypted_key | 사용자별 API 키(암호화 저장) |
| `usage` | id, user_id(FK), date, total_tokens, total_calls | 사용량 추적 |
| `settings` | user_id(FK), theme, defaults(JSON) | 사용자 설정 |
| `answer_cache` | id, question, question_embedding(vector), final_answer, agent_events(JSON), hit_count, created_at | 답변 캐시(15.4) |

### 15.4 답변 캐싱 (임베딩 유사도 기반)

- **키 기준**: 질문 정확 일치가 아니라 **의미 유사도**. 신규 질문을 임베딩한 뒤
  `answer_cache`에서 코사인 최근접 검색(`pgvector`)으로 가장 유사한 항목을 찾는다.
- **흐름**:
  ```
  [/api/query 진입]
        → 질문 임베딩 생성
        → pgvector 최근접 검색 (cosine)
        → 최고 유사도 ≥ THRESHOLD(예: 0.95) ?
              ⭕ 캐시 HIT  → 저장된 final_answer + agent_events를 SSE로 즉시 재생, hit_count++
              ❌ 캐시 MISS → 기존 4-Agent 토론 실행 → 결과를 answer_cache에 저장(임베딩 포함)
  ```
- **임베딩 모델**: OpenAI 호환 Gateway의 임베딩 엔드포인트(`text-embedding-3-small` 등)
  또는 로컬 sentence-transformer. 차원은 선택 모델에 맞춰 `vector(N)`로 고정.
- **주의(연구 무결성)**: 캐싱은 **데모 서비스 경로(`/api/query`)에만** 적용한다.
  실험 파이프라인(`experiments/`)은 매 실행 독립 측정이 필요하므로 캐시를 우회한다.

### 15.5 폴더 구조 추가분 (10장 보강)

```
backend/
├── db/
│   ├── database.py          # async engine, session
│   ├── models.py            # SQLAlchemy ORM 모델
│   └── migrations/          # Alembic
├── auth/
│   ├── oauth.py             # Authlib Google OAuth
│   └── jwt.py               # 토큰 발급/검증
└── services/
    ├── conversation.py      # 대화 영구화 CRUD
    └── cache.py             # 임베딩 캐싱 로직
```

---

## 부록: 참고 논문 (Citation 형식)

```
[1] Ye, R., Liu, X., Wu, Q., Pang, X., Yin, Z., Bai, L., & Chen, S. (2025). 
    X-MAS: Towards Building Multi-Agent Systems with Heterogeneous LLMs. 
    arXiv:2505.16997.

[2] Yang, Y., Qu, C., Wen, M., Shi, L., Wen, Y., Zhang, W., Wierman, A., & 
    Gu, S. (2026). Understanding Agent Scaling in LLM-Based Multi-Agent 
    Systems via Diversity. arXiv:2602.03794.

[3] Tran, D., & Kiela, D. (2026). Single-Agent LLMs Outperform Multi-Agent 
    Systems on Multi-Hop Reasoning Under Equal Thinking Token Budgets. 
    arXiv:2604.02460. Stanford University.

[4] Cemri, M., et al. (2025). Why Do Multi-Agent LLM Systems Fail? 
    NeurIPS 2025 Datasets and Benchmarks Track.

[5] Trivedi, H., Balasubramanian, N., Khot, T., & Sabharwal, A. (2022). 
    MuSiQue: Multihop Questions via Single-hop Question Composition. 
    TACL.
```

---

*문서 작성일: 2026-05-11*  
*작성: AI소프트웨어학부 김가람 + Claude (Anthropic)*
